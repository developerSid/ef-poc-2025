#!/usr/bin/env bash
source "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/_payeredi_common.sh"

require_command docker

compose --profile db up -d vadb
wait_for_database
compose --profile db run --rm --no-deps db-init

printf 'PayerEdi database is ready.\n'
