#!/usr/bin/env bash
source "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/_payeredi_common.sh"

require_command docker

compose --profile db down --remove-orphans
printf 'PayerEDI containers stopped; the vadb volume was preserved.\n'
