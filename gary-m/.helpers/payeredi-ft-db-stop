#!/usr/bin/env bash
source "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/_payeredi_common.sh"

require_command docker

compose --profile test-db down --remove-orphans
printf 'Functional-test SQL Server stopped; tmpfs data was discarded.\n'
